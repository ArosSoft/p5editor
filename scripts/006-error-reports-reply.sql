-- ============================================================
-- p5editor — SQL миграция: ответы на сообщения об ошибках
-- Версия: 0.5.5
-- Дата: 21 августа 2026 г.
-- ============================================================

-- Добавляем поля для ответа модератора/администратора на сообщение.
-- Если таблица создавалась свежей миграцией 005, эти колонки уже присутствуют
-- (CREATE TABLE выше их содержит). Этот скрипт покрывает случай уже созданной таблицы.

ALTER TABLE public.error_reports
  ADD COLUMN IF NOT EXISTS reply TEXT,
  ADD COLUMN IF NOT EXISTS replied_at TIMESTAMPTZ;

-- ============================================================
-- Конец миграции
-- ============================================================

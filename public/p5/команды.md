## Структура скетча

- `setup()` — выполняется один раз при запуске скетча.[^2]
- `draw()` — выполняется циклически для отрисовки кадра.[^1]
- `preload()` — заранее загружает ресурсы до `setup()`.[^1]
- `remove()` — удаляет sketch и связанные элементы.[^1]
- `noLoop()` — останавливает цикл `draw()`.[^1]
- `loop()` — снова запускает цикл `draw()`.[^1]
- `redraw()` — вручную вызывает `draw()` один раз.[^1]
- `push()` — сохраняет текущие настройки рисования.[^1]
- `pop()` — восстанавливает сохранённые настройки рисования.[^1]


## Canvas и окно

- `createCanvas()` — создаёт холст для рисования.[^2]
- `resizeCanvas()` — изменяет размер холста.[^1]
- `noCanvas()` — удаляет холст.[^1]
- `frameRate()` — задаёт или получает частоту кадров.[^1]
- `frameCount` — количество кадров с начала работы программы (свойство).[^1]
- `pixelDensity()` — управляет плотностью пикселей.[^1]
- `displayDensity()` — возвращает плотность пикселей экрана.[^1]
- `fullscreen()` — включает или проверяет полноэкранный режим.[^1]
- `smooth()` — включает сглаживание.[^1]
- `noSmooth()` — отключает сглаживание.[^1]
- `width` — ширина холста (свойство).[^1]
- `height` — высота холста (свойство).[^1]
- `windowWidth` — ширина окна браузера (свойство).[^1]
- `windowHeight` — высота окна браузера (свойство).[^1]
- `displayWidth` — ширина экрана устройства (свойство).[^1]
- `displayHeight` — высота экрана устройства (свойство).[^1]
- `blendMode()` — устанавливает режим смешивания цветов.[^1]
- `noCursor()` — скрывает курсор.[^1]
- `getURL()` — возвращает текущий URL.[^1]
- `getURLPath()` — возвращает путь URL как массив.[^1]
- `getURLParams()` — возвращает параметры URL как объект.[^1]
- `windowResized()` — вызывается при изменении размера окна.[^1]


## Фон и очистка

- `background()` — закрашивает фон.[^1]
- `clear()` — очищает холст прозрачным цветом.[^1]


## Линии и фигуры

- `line()` — рисует линию.[^1]
- `point()` — рисует точку.[^1]
- `triangle()` — рисует треугольник.[^1]
- `rect()` — рисует прямоугольник.[^1]
- `square()` — рисует квадрат.[^1]
- `ellipse()` — рисует эллипс.[^1]
- `circle()` — рисует круг.[^1]
- `arc()` — рисует дугу.[^1]
- `quad()` — рисует четырёхугольник.[^1]
- `bezier()` — рисует кривую Безье.[^1]
- `curve()` — рисует кривую Катмулла-Рома.[^1]
- `beginShape()` — начинает сложную фигуру.[^1]
- `endShape()` — завершает сложную фигуру.[^1]
- `vertex()` — добавляет вершину.[^1]
- `curveVertex()` — добавляет вершину для кривой.[^1]
- `bezierVertex()` — добавляет сегмент Безье.[^1]
- `quadraticVertex()` — добавляет квадратичную вершину Безье.[^1]
- `beginContour()` — начинает внутренний контур.[^1]
- `endContour()` — завершает внутренний контур.[^1]
- `ellipseMode()` — устанавливает режим отрисовки эллипсов.[^1]
- `rectMode()` — устанавливает режим отрисовки прямоугольников.[^1]
- `strokeWeight()` — задаёт толщину линии.[^1]
- `strokeCap()` — задаёт форму концов линии.[^1]
- `strokeJoin()` — задаёт стиль соединения линий.[^1]
- `bezierDetail()` — устанавливает детализацию кривых Безье.[^1]
- `bezierPoint()` — вычисляет точку на кривой Безье.[^1]
- `curveDetail()` — устанавливает детализацию кривых.[^1]
- `curveTightness()` — устанавливает натяжение кривых.[^1]
- `curvePoint()` — вычисляет точку на сплайн-кривой.[^1]


## 3D-объекты

- `box()` — рисует 3D-куб/параллелепипед.[^1]
- `plane()` — рисует плоскость.[^1]
- `sphere()` — рисует сферу.[^1]
- `cylinder()` — рисует цилиндр.[^1]
- `cone()` — рисует конус.[^1]
- `ellipsoid()` — рисует эллипсоид.[^1]
- `torus()` — рисует тор.[^1]
- `loadModel()` — загружает 3D-модель из файла.[^1]
- `model()` — рисует 3D-модель.[^1]


## Закраска и обводка

- `fill()` — задаёт цвет заливки.[^1]
- `noFill()` — отключает заливку.[^1]
- `stroke()` — задаёт цвет обводки.[^1]
- `noStroke()` — отключает обводку.[^1]
- `strokeWeight()` — задаёт толщину линии.[^1]
- `strokeCap()` — задаёт форму концов линии.[^1]
- `strokeJoin()` — задаёт стиль соединения линий.[^1]
- `tint()` — задаёт оттенок для изображений.[^1]
- `noTint()` — отключает оттенок.[^1]
- `erase()` — включает режим стирания.[^1]
- `noErase()` — отключает режим стирания.[^1]


## Трансформации

- `translate()` — сдвигает систему координат.[^1]
- `rotate()` — поворачивает систему координат.[^1]
- `rotateX()` — поворот вокруг оси X.[^1]
- `rotateY()` — поворот вокруг оси Y.[^1]
- `rotateZ()` — поворот вокруг оси Z.[^1]
- `scale()` — масштабирует.[^1]
- `shearX()` — сдвиг по оси X.[^1]
- `shearY()` — сдвиг по оси Y.[^1]
- `applyMatrix()` — применяет матрицу преобразования.[^1]
- `resetMatrix()` — сбрасывает преобразования.[^1]


## Цвет

- `color()` — создаёт цвет.[^1]
- `lerpColor()` — смешивает два цвета.[^1]
- `colorMode()` — задаёт цветовую модель.[^1]
- `red()` — возвращает красную составляющую.[^1]
- `green()` — возвращает зелёную составляющую.[^1]
- `blue()` — возвращает синюю составляющую.[^1]
- `alpha()` — возвращает прозрачность.[^1]
- `hue()` — возвращает оттенок.[^1]
- `saturation()` — возвращает насыщенность.[^1]
- `brightness()` — возвращает яркость.[^1]
- `lightness()` — возвращает светлоту.[^1]
- `erase()` — включает режим стирания.[^1]
- `noErase()` — отключает режим стирания.[^1]


## Текст

- `text()` — рисует текст.[^1]
- `textFont()` — задаёт шрифт.[^1]
- `textSize()` — задаёт размер шрифта.[^1]
- `textStyle()` — задаёт стиль текста.[^1]
- `textWidth()` — измеряет ширину текста.[^1]
- `textAscent()` — возвращает высоту над базовой линией.[^1]
- `textDescent()` — возвращает высоту под базовой линией.[^1]
- `textAlign()` — задаёт выравнивание текста.[^1]
- `textLeading()` — задаёт межстрочный интервал.[^1]
- `loadFont()` — загружает шрифт как p5.Font.[^1]
- `textBounds()` — возвращает ограничивающую рамку текста.[^1]


## Изображения и пиксели

- `loadImage()` — загружает изображение.[^1]
- `image()` — рисует изображение на холсте.[^1]
- `imageMode()` — устанавливает режим отрисовки изображений.[^1]
- `resize()` — изменяет размер изображения.[^1]
- `copy()` — копирует участок изображения.[^1]
- `mask()` — применяет маску.[^1]
- `blend()` — смешивает изображения.[^1]
- `get()` — получает пиксель или участок изображения.[^1]
- `set()` — задаёт пиксель или участок.[^1]
- `loadPixels()` — загружает массив пикселей.[^1]
- `updatePixels()` — применяет изменения пикселей.[^1]
- `filter()` — применяет фильтр к изображению/холсту.[^1]
- `saveCanvas()` — сохраняет холст как изображение.[^1]
- `save()` — сохраняет данные или изображение.[^1]


## Материал и свет

- `ambientLight()` — создаёт окружающий свет.[^1]
- `directionalLight()` — создаёт направленный свет.[^1]
- `pointLight()` — создаёт точечный свет.[^1]
- `spotLight()` — создаёт направленный прожектор.[^1]
- `lights()` — включает стандартное освещение.[^1]
- `noLights()` — отключает все источники света.[^1]
- `lightFalloff()` — устанавливает затухание света.[^1]
- `specularColor()` — устанавливает цвет зеркального блика.[^1]
- `normalMaterial()` — использует нормали для окраски.[^1]
- `ambientMaterial()` — задаёт окружающий материал.[^1]
- `emissiveMaterial()` — задаёт излучающий материал.[^1]
- `specularMaterial()` — задаёт зеркальный материал.[^1]
- `shininess()` — задаёт блеск поверхности.[^1]
- `texture()` — применяет текстуру к геометрии.[^1]


## Камера и проекция

- `camera()` — задаёт положение камеры.[^1]
- `perspective()` — задаёт перспективную проекцию.[^1]
- `ortho()` — задаёт ортографическую проекцию.[^1]
- `createCamera()` — создаёт камеру.[^1]
- `setCamera()` — устанавливает активную камеру.[^1]
- `orbitControl()` — включает управление камерой мышью.[^1]
- `debugMode()` — включает режим отладки.[^1]
- `noDebugMode()` — отключает режим отладки.[^1]


## Математика

- `random()` — возвращает случайное число.[^1]
- `randomSeed()` — задаёт зерно случайности.[^1]
- `randomGaussian()` — возвращает случайное число по Гауссу.[^1]
- `noise()` — возвращает шум Перлина.[^1]
- `noiseSeed()` — задаёт зерно шума.[^1]
- `noiseDetail()` — настраивает шум.[^1]
- `map()` — переводит число из одного диапазона в другой.[^1]
- `dist()` — вычисляет расстояние.[^1]
- `lerp()` — линейно интерполирует.[^1]
- `constrain()` — ограничивает значение диапазоном.[^1]
- `mag()` — вычисляет длину вектора.[^1]
- `sq()` — возводит в квадрат.[^1]
- `sqrt()` — извлекает корень.[^1]
- `pow()` — возводит в степень.[^1]
- `abs()` — модуль числа.[^1]
- `ceil()` — округляет вверх.[^1]
- `floor()` — округляет вниз.[^1]
- `round()` — округляет до ближайшего целого.[^1]
- `log()` — натуральный логарифм.[^1]
- `fract()` — дробная часть числа.[^1]
- `min()` — находит минимум.[^1]
- `max()` — находит максимум.[^1]
- `sin()` — синус.[^1]
- `cos()` — косинус.[^1]
- `tan()` — тангенс.[^1]
- `asin()` — арксинус.[^1]
- `acos()` — арккосинус.[^1]
- `atan()` — арктангенс.[^1]
- `atan2()` — арктангенс с двумя аргументами.[^1]
- `degrees()` — переводит радианы в градусы.[^1]
- `radians()` — переводит градусы в радианы.[^1]
- `angleMode()` — устанавливает режим углов (DEGREES/RADIANS).[^1]
- `createVector()` — создаёт новый вектор p5.Vector.[^1]


## Массивы и строки

- `append()` — добавляет элемент в массив.[^1]
- `arrayCopy()` — копирует элементы между массивами.[^1]
- `concat()` — объединяет массивы.[^1]
- `reverse()` — переворачивает массив.[^1]
- `shorten()` — укорачивает массив.[^1]
- `shuffle()` — перемешивает массив.[^1]
- `sort()` — сортирует массив.[^1]
- `splice()` — вставляет или удаляет элементы.[^1]
- `subset()` — берёт подмассив.[^1]
- `join()` — объединяет элементы в строку.[^1]
- `split()` — разделяет строку.[^1]
- `splitTokens()` — разделяет строку по разделителям.[^1]
- `trim()` — удаляет пробелы по краям строки.[^1]
- `nf()` — форматирует числа.[^1]
- `nfc()` — форматирует числа с разделителями тысяч.[^1]
- `nfp()` — форматирует числа со знаком.[^1]
- `nfs()` — форматирует числа с пробелом для знака.[^1]
- `float()` — преобразует в число с плавающей точкой.[^1]
- `int()` — преобразует в целое число.[^1]
- `str()` — преобразует в строку.[^1]
- `boolean()` — преобразует в булево значение.[^1]
- `byte()` — преобразует в байт.[^1]
- `char()` — преобразует в символ.[^1]
- `unchar()` — преобразует символ в код.[^1]
- `hex()` — преобразует в шестнадцатеричную строку.[^1]
- `unhex()` — преобразует из шестнадцатеричной строки.[^1]
- `createStringDict()` — создаёт строковый словарь.[^1]
- `createNumberDict()` — создаёт числовой словарь.[^1]


## Объекты и JSON

- `createObject()` — создаёт объект p5.[^1]
- `saveJSON()` — сохраняет JSON.[^1]
- `loadJSON()` — загружает JSON.[^1]
- `loadStrings()` — загружает текстовый файл построчно.[^1]
- `loadXML()` — загружает XML файл.[^1]
- `loadBytes()` — загружает файл как байты.[^1]
- `httpGet()` — выполняет HTTP GET запрос.[^1]
- `httpPost()` — выполняет HTTP POST запрос.[^1]
- `httpDo()` — выполняет HTTP запрос.[^1]
- `createWriter()` — создаёт объект для записи в файл.[^1]
- `saveStrings()` — сохраняет массив строк в файл.[^1]
- `storeItem()` — сохраняет значение в localStorage.[^1]
- `getItem()` — получает значение из localStorage.[^1]
- `removeItem()` — удаляет значение из localStorage.[^1]
- `clearStorage()` — очищает localStorage.[^1]


## Таблицы

- `loadTable()` — загружает таблицу.[^1]
- `createTable()` — создаёт таблицу.[^1]
- `saveTable()` — сохраняет таблицу.[^1]
- `Table` — класс таблицы.[^1]
- `TableRow` — строка таблицы.[^1]
- `TableColumn` — столбец таблицы.[^1]


## Ввод

- `mouseX` — текущая X-координата мыши.[^3][^4]
- `mouseY` — текущая Y-координата мыши.[^4][^3]
- `pmouseX` — X-координата мыши в прошлом кадре.[^3]
- `pmouseY` — Y-координата мыши в прошлом кадре.[^3]
- `winMouseX` — X-координата мыши относительно окна.[^1]
- `winMouseY` — Y-координата мыши относительно окна.[^1]
- `mouseIsPressed` — true, пока нажата кнопка мыши.[^4][^3]
- `mouseButton` — какая кнопка мыши нажата последней.[^5]
- `key` — последняя нажатая клавиша.[^5]
- `keyCode` — код последней клавиши.[^1]
- `keyIsPressed` — true, пока нажата клавиша.[^5]
- `keyIsDown()` — проверяет, нажата ли клавиша с данным кодом.[^1]
- `touches` — массив касаний на сенсорном экране.[^1]
- `keyPressed()` — срабатывает при нажатии клавиши.[^1]
- `keyReleased()` — срабатывает при отпускании клавиши.[^1]
- `keyTyped()` — срабатывает при вводе символа.[^1]
- `doubleClicked()` — срабатывает при двойном клике.[^1]
- `mousePressed()` — срабатывает при нажатии мыши.[^3]
- `mouseReleased()` — срабатывает при отпускании мыши.[^1]
- `mouseClicked()` — срабатывает при клике мышью.[^1]
- `mouseMoved()` — срабатывает при движении мыши.[^1]
- `mouseDragged()` — срабатывает при перетаскивании мыши.[^1]
- `mouseWheel()` — срабатывает при прокрутке колесика.[^1]
- `touchStarted()` — срабатывает при начале касания.[^1]
- `touchMoved()` — срабатывает при движении пальца.[^1]
- `touchEnded()` — срабатывает при завершении касания.[^1]
- `windowResized()` — срабатывает при изменении размера окна.[^1]


## DOM-элементы

- `createDiv()` — создаёт блок `div`.[^1]
- `createP()` — создаёт абзац.[^1]
- `createSpan()` — создаёт `span`.[^1]
- `createImg()` — создаёт изображение.[^1]
- `createA()` — создаёт ссылку.[^1]
- `createSlider()` — создаёт ползунок.[^1]
- `createButton()` — создаёт кнопку.[^1]
- `createCheckbox()` — создаёт флажок.[^1]
- `createSelect()` — создаёт выпадающий список.[^1]
- `createRadio()` — создаёт переключатели.[^1]
- `createInput()` — создаёт поле ввода.[^1]
- `createFileInput()` — создаёт загрузку файла.[^1]
- `createColorPicker()` — создаёт выбор цвета.[^1]
- `createVideo()` — создаёт видео элемент.[^1]
- `createAudio()` — создаёт аудио элемент.[^1]
- `createElement()` — создаёт произвольный элемент.[^1]
- `select()` — находит элемент по селектору.[^1]
- `selectAll()` — находит все элементы по селектору.[^1]
- `removeElements()` — удаляет все DOM-элементы, созданные через p5.[^1]
- `html()` — изменяет HTML-содержимое элемента.[^1]
- `value()` — получает или задаёт значение элемента.[^1]
- `style()` — изменяет стиль элемента.[^1]
- `remove()` — удаляет DOM-элемент.[^1]
- `changed()` — вызывается при изменении значения элемента.[^1]
- `input()` — вызывается при вводе данных в элемент.[^1]


## Аудио

- `loadSound()` — загружает звуковой файл.[^1]
- `soundFormats()` — задаёт поддерживаемые форматы звука.[^1]
- `userStartAudio()` — запускает аудио по действию пользователя.[^1]
- `AudioIn` — объект входного аудиосигнала.[^1]
- `SoundFile` — класс звукового файла.[^1]
- `fft` — анализ частотного спектра.[^1]
- `Amplitude` — анализ амплитуды.[^1]


## Время

- `millis()` — время в миллисекундах с запуска.[^1]
- `second()` — текущая секунда.[^1]
- `minute()` — текущая минута.[^1]
- `hour()` — текущий час.[^1]
- `day()` — текущий день месяца.[^1]
- `month()` — текущий месяц.[^1]
- `year()` — текущий год.[^1]
- `week()` — номер недели.[^1]


## Утилиты

- `print()` — выводит данные в консоль.[^1]
- `save()` — сохраняет данные в файл.[^1]
- `saveCanvas()` — сохраняет холст как изображение.[^1]
- `saveFrames()` — сохраняет последовательность кадров.[^1]
- `cursor()` — задаёт вид курсора.[^1]
- `showCursor()` — показывает курсор.[^1]
- `hideCursor()` — скрывает курсор.[^1]


## Константы

- `CENTER` — выравнивание по центру.[^1]
- `CORNER` — координаты от угла.[^1]
- `CORNERS` — координаты по двум углам.[^1]
- `RADIUS` — координаты по радиусу.[^1]
- `LEFT` — выравнивание влево.[^1]
- `RIGHT` — выравнивание вправо.[^1]
- `TOP` — выравнивание сверху.[^1]
- `BOTTOM` — выравнивание снизу.[^1]
- `BASELINE` — выравнивание по базовой линии.[^1]
- `OPEN` — открытая фигура.[^1]
- `CLOSE` — замкнутая фигура.[^1]
- `PI` — число $\pi$.[^1]
- `HALF_PI` — половина $\pi$.[^1]
- `QUARTER_PI` — четверть $\pi$.[^1]
- `TAU` — $2\pi$.[^1]
- `TWO_PI` — $2\pi$.[^1]
- `DEGREES` — градусы.[^1]
- `RADIANS` — радианы.[^1]
- `RGB` — цветовая модель RGB.[^1]
- `HSB` — цветовая модель HSB.[^1]
- `HSL` — цветовая модель HSL.[^1]
- `WEBGL` — режим WebGL.[^1]
- `BLEND` — обычное смешивание.[^1]
- `DARKEST` — затемнение.[^1]
- `LIGHTEST` — осветление.[^1]
- `DIFFERENCE` — разность.[^1]
- `MULTIPLY` — умножение.[^1]
- `SCREEN` — экранное смешивание.[^1]
- `OVERLAY` — наложение.[^1]
- `HARD_LIGHT` — жёсткий свет.[^1]
- `SOFT_LIGHT` — мягкий свет.[^1]
- `DODGE` — осветление.[^1]
- `BURN` — затемнение.[^1]
- `ADD` — сложение.[^1]
- `REMOVE` — удаление.[^1]
- `EXCLUSION` — исключение.[^1]
- `NORMAL` — нормальный режим.[^1]
- `THRESHOLD` — пороговый эффект.[^1]
- `GRAY` — оттенки серого.[^1]
- `OPAQUE` — непрозрачный.[^1]
- `INVERT` — инверсия.[^1]
- `POSTERIZE` — постеризация.[^1]
- `DILATE` — расширение.[^1]
- `ERODE` — эрозия.[^1]
- `ARROW` — курсор-стрелка.[^1]
- `CROSS` — курсор-крест.[^1]
- `HAND` — курсор-рука.[^1]
- `MOVE` — курсор перемещения.[^1]
- `TEXT` — текстовый курсор.[^1]
- `WAIT` — курсор ожидания.[^1]
- `AUTO` — автоматический режим.[^1]
- `ALT` — клавиша Alt.[^1]
- `BACKSPACE` — клавиша Backspace.[^1]
- `CONTROL` — клавиша Ctrl.[^1]
- `DELETE` — клавиша Delete.[^1]
- `DOWN_ARROW` — стрелка вниз.[^1]
- `ENTER` — клавиша Enter.[^1]
- `ESCAPE` — клавиша Esc.[^1]
- `LEFT_ARROW` — стрелка влево.[^1]
- `OPTION` — клавиша Option.[^1]
- `RETURN` — клавиша Return.[^1]
- `RIGHT_ARROW` — стрелка вправо.[^1]
- `SHIFT` — клавиша Shift.[^1]
- `TAB` — клавиша Tab.[^1]
- `UP_ARROW` — стрелка вверх.[^1]
- `P2D` — 2D-рендерер.[^1]
- `P3D` — 3D-рендерер.[^1]


## Версия

- `VERSION` — текущая версия p5.js.[^6]

// Напиши этот код в функции draw() на следующей строке
// Добавляем новые пузырьки
  if (random() < 0.1) {                    // 10% вероятности каждый кадр
    bubbles.push({
      x: random(width),                    // Случайная позиция по ширине
      y: height,                            // Начинаем снизу (y = 400)
      size: random(5, 15),                  // Размер от 5 до 15
      speed: random(1, 3)                    // Скорость всплытия
    });
  }
Что это: Примерно 6 раз в секунду 
создаём новый пузырёк внизу экрана.
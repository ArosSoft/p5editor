 // Пиши в цикле for
 fill(fishy.color);                  
 // Цвет рыбки
    noStroke();                          
    // Без обводки
    
    // Тело рыбки (овал)
    ellipse(fishy.x, fishy.y, fishy.size, fishy.size * 0.6);

Что это: Рисуем овальное тело рыбки (шире, чем высота).


//1. Добавление падающих звёзд

//1.2.С вероятностью 1% (когда random() < 0.01) создаётся новая падающая звезда.

//1.3. Она появляется вверху экрана (y = 0), с разной горизонтальной скоростью (может лететь немного вбок) и вертикальной скоростью (падает вниз). «Жизнь» новой звезды = 255 (полная яркость).

if (random() < 0.01) {
    shootingStars.push({
      x: random(width),
      y: 0,
      vx: random(-2, 2),
      vy: random(5, 10),
      life: 255
    });
  }
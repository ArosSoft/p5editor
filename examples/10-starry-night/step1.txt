//Подготовка к рисованию (функция setup)
//1. Создаёт холст размером 400×400 пикселей.
//2. Создаёт пустой список (массив) stars, в котором будут храниться все мерцающие звёзды.
//3. Все эти данные сохраняются в массиве stars.


let stars = [];
let shootingStars = [];

function setup() {
  createCanvas(400, 400);
  

  // Пиши код в функции draw()
  // Рисуем рыбок
  for (let fishy of fish) {           
    // Перебираем всех рыбок
    fishy.x += fishy.speed;            
    // Двигаем вправо
    
    if (fishy.x > width + 50) {        
    // Если уплыла далеко вправо
      fishy.x = -50;                    
      // Появляется слева
    }
  }
Что это: Рыбки плывут вправо. 
Когда уплывают за экран, 
появляются снова слева.


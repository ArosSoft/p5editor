    // Напиши дальше в for
    // Глаз (чёрная точка)
    fill(0);                             // Чёрный цвет
    ellipse(fishy.x + fishy.size * 0.2, fishy.y - 2, 3, 3);
    
    // Хвост (треугольник)
    fill(fishy.color);                    // Тот же цвет, что и тело
    triangle(fishy.x - fishy.size / 2, fishy.y,      // Левая точка
             fishy.x - fishy.size / 2 - 10, fishy.y - 5,  // Верхняя точка
             fishy.x - fishy.size / 2 - 10, fishy.y + 5);  // Нижняя точка
 
Что это: Добавляем детали - чёрный глаз 
и треугольный хвост слева от тела.
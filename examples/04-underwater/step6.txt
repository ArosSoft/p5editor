  // пиши код в той-же функции
  // Рисуем пузырьки
  for (let i = bubbles.length - 1; i >= 0; i--) {  // Идём с конца списка
    let b = bubbles[i];     // Берём текущий пузырёк
    b.y -= b.speed;         // Поднимаем вверх (уменьшаем Y)
    
    fill(255, 255, 255, 150);  // Белый полупрозрачный цвет
    noStroke();                 // Без обводки
    ellipse(b.x, b.y, b.size, b.size);  // Рисуем кружок
    
    if (b.y < 0) {            // Если пузырёк улетел за верх экрана
      bubbles.splice(i, 1);    // Удаляем его
    }
  }
Что это: Каждый пузырёк поднимается 
вверх со своей скоростью. Улетевшие 
за экран удаляются.
# Урок 6: Оживляем снеговика (Анимация)

Самое интересное в программировании — это анимация. Давай заставим нашего снеговика махать нам рукой!

## Как работает анимация
Вспомни, что функция `draw()` выполняется снова и снова. Если мы будем понемногу менять координаты руки в каждом кадре, нам покажется, что она движется.

## Добавляем переменную
Переменная — это контейнер для хранения числа. Мы создадим переменные `отклонение` и `направление`, которые будут изменяться.

1.  **В самом верху**, до функции `setup()`, добавь:
    ```javascript
    let отклонение = 0;
    let направление = 1;
    ```

2.  **В функции `draw()`**, перед рисованием рук, добавим логику движения:
    ```javascript
    // Анимация для руки
    отклонение = отклонение + waveDirection;
    if (отклонение > 20 || отклонение < -20) {
      направление = направление * -1; // Меняем направление
    }
    ```

3.  **Теперь изменим код правой руки**. Вместо фиксированной точки (300, 140) подставим нашу переменную.
    *Было:* `line(240, 170, 300, 140);`
    *Стало:*
    ```javascript
    line(240, 170, 300 + отклонение, 140 + отклонение/2);
    ```

![картинка 6](images/16.png)

Запусти программу. Видишь? Правая рука теперь двигается вверх-вниз, но пальцы остаются на месте! 

### Задание для настоящих программистов
Добавь переменные также для координат пальцев, чтобы они двигались вместе с рукой рядом с той же скоростью.